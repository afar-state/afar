{
  "name": "fetch-session-injector",
  "$schema": "https://www.unpkg.com/nx/schemas/project-schema.json",
  "sourceRoot": "libs/fetch-session-injector/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/rollup:rollup",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/fetch-session-injector",
        "main": "libs/fetch-session-injector/src/index.ts",
        "tsConfig": "libs/fetch-session-injector/tsconfig.lib.json",
        "updateBuildableProjectDepsInPackageJson": true,
        "project": "libs/fetch-session-injector/package.json",
        "format": [
          "esm"
        ],
        "assets": []
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs fetch-session-injector {args.ver} {args.tag}"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/fetch-session-injector/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/fetch-session-injector/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
